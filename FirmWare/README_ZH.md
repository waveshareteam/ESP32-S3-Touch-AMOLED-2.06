# 固件

[English](README.md)

本目录包含该开发板已提交的出厂或恢复固件映像。它们不由 CI 生成，也不被构建工作流视为源码
项目。仅在产品文档或支持流程要求刷写已发布二进制文件时使用。

维护中的 ESP-IDF 和 Arduino 源码项目会由 `Build Examples` 工作流生成固件工件；可使用
`releases/` 中的工具打包构建输出或下载 CI 工件。

# Firmware

This directory contains checked-in factory or recovery firmware images for the board.

These binaries are not generated by CI and are not treated as source projects by the build workflows. Use them only when the product documentation or support flow calls for flashing a released binary image.

Source-built firmware artifacts should be produced by GitHub Actions from maintained ESP-IDF or Arduino source projects and uploaded as workflow artifacts when release packaging is added.
